name: CI - Build, Push, Update GitOps

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]

concurrency:
  group: poo-diary-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  GITOPS_REPOSITORY: uhjinkim9/helm-chart
  CHART_PATH: charts/namespace-poo-diary/poo-diary

jobs:
  build-and-push:
    name: Build & Push API/Web
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract API image metadata
        id: meta-api
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/poo-diary-api
          tags: |
            type=sha,prefix=${{ github.ref_name }}-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-api.outputs.tags }}
          labels: ${{ steps.meta-api.outputs.labels }}
          cache-from: type=gha,scope=api
          cache-to: type=gha,scope=api,mode=max

      - name: Extract Web image metadata
        id: meta-web
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/poo-diary-web
          tags: |
            type=sha,prefix=${{ github.ref_name }}-,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push Web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-web.outputs.tags }}
          labels: ${{ steps.meta-web.outputs.labels }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,scope=web,mode=max

  update-gitops:
    name: Update Helm image tags
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/download/v4.45.4/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Checkout GitOps repository
        uses: actions/checkout@v4
        with:
          repository: ${{ env.GITOPS_REPOSITORY }}
          ref: main
          token: ${{ secrets.GITOPS_TOKEN }}

      - name: Update API and Web tags
        env:
          IMAGE_TAG: main-${{ github.sha }}
        run: |
          set -euo pipefail
          IMAGE_TAG="${IMAGE_TAG:0:12}"
          VALUES_FILE="${CHART_PATH}/values.yaml"

          test -f "${VALUES_FILE}"
          yq -i '.api.image.tag = strenv(IMAGE_TAG)' "${VALUES_FILE}"
          yq -i '.web.image.tag = strenv(IMAGE_TAG)' "${VALUES_FILE}"

          test "$(yq '.api.image.tag' "${VALUES_FILE}")" = "${IMAGE_TAG}"
          test "$(yq '.web.image.tag' "${VALUES_FILE}")" = "${IMAGE_TAG}"

          echo "API/Web image tag: ${IMAGE_TAG}"

      - name: Commit and push Helm values
        env:
          SOURCE_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          VALUES_FILE="${CHART_PATH}/values.yaml"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "${VALUES_FILE}"

          if git diff --cached --quiet; then
            echo "이미 최신 태그입니다."
            exit 0
          fi

          git commit -m "ci: deploy poo-diary ${SOURCE_SHA:0:7} [skip ci]"
          git push origin main

  notify:
    name: Discord notification
    if: always() && github.event_name != 'pull_request'
    needs: [build-and-push, update-gitops]
    runs-on: ubuntu-latest

    steps:
      - name: Notify success
        if: needs.build-and-push.result == 'success' && needs.update-gitops.result != 'failure'
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: success
          title: "✅ 빌드 성공: poo-diary"
          description: |
            **태그:** `main-${{ github.sha }}`
            **커밋:** `${{ github.sha }}`
            Argo CD 자동 배포가 시작됩니다.
          color: 0x57F287

      - name: Notify failure
        if: needs.build-and-push.result == 'failure' || needs.update-gitops.result == 'failure'
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: failure
          title: "❌ 빌드 실패: poo-diary"
          description: |
            **로그:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          color: 0xED4245
